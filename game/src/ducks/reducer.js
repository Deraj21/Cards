let initialState = {
  deck: { cards: [] },
  players: [],
  currentPlayer: null
};

// action types

function reducer(state = initialState, action){
  switch (action.type){
    default:
      return state;
  }
}

// action creators

export default reducer;